# Core (versions pinned to match the manuscript, Section 4).
torch==2.1.0
torchvision==0.16.0
numpy>=1.23,<2.0
PyYAML>=6.0
scikit-image>=0.21
Pillow>=9.0
thop>=0.1.1            # parameter / MACs analysis (Table 8)
tqdm>=4.65

# Notes
# -----
# * The exact CUDA build of torch/torchvision should be selected to match your
#   GPU and driver (see https://pytorch.org/get-started/previous-versions/).
# * scikit-image is used for fast PNG reading; the dataset loader falls back to
#   Pillow if scikit-image is unavailable.
